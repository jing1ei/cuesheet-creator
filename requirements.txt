# cuesheet-creator — core dependency versions
# This file covers required (core) packages only.
# Optional component versions (scene/asr/ocr) are maintained in
# scripts/cc/constants.py OPTIONAL_COMPONENTS and installed via
# prepare-env / install-deps commands.
# Manual core install: pip install -r requirements.txt

# Required (core functionality)
opencv-python-headless>=4.8,<5
numpy>=1.24
Pillow>=10.0,<12
openpyxl>=3.1,<4

# Optional: enhanced scene detection
# scenedetect[opencv]>=0.6,<1

# Optional: ASR speech recognition
# faster-whisper>=1.0,<2

# Optional: OCR text detection
# Primary engine (installed by install-ocr):
# rapidocr-onnxruntime>=1.3,<2
#
# Alternative engines (install manually if preferred; runtime auto-detects):
# easyocr>=1.7,<2
# paddleocr>=2.7,<3
